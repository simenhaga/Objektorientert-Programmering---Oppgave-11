import java.awt.*;

public class Square extends Rectangle{
    public Square(Color color, boolean isFilled, float height, float width, Point topLeft, Point topRight) {
        super(color, isFilled, height, width, topLeft, topRight);
    }
}
